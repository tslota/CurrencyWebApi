A simple example of using a NBP api for reading currency data.
Application is using a .net core 3.1.

To build a source, run: dotnet build 
To runs it on Windows just call CurrencyWinApi.exe on other systems call dotnet CurrencyWinApi.dll

Open browser and go to address http://localhost:5000/swagger to call a test method.